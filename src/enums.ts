export enum gamepadDirection {
    up = "up",
    down = "down",
    left = "left",
    right = "right",
}

export enum gamepadButtonType {
    onOff = "onOff",
    variable = "variable",
}

export enum gamepadEmulationState {
    real = "real",
    emulated = "emulated",
    overlay = "overlay",
}
